package com.edutech.users.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class MapperConfig {
    // La configuración está vacía porque los mappers se configuran mediante anotaciones
    // en las interfaces respectivas como RoleMapper, UserMapper, etc.
}
